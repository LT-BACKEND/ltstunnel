```
apt-get update && apt-get upgrade -y && apt dist-upgrade -y && update-grub && reboot
```

```
sudo apt-get install gnupg -y && sudo apt install iptables && wget https://raw.githubusercontent.com/LT-BACKEND/ltstunnel/main/installer/install-script.sh && chmod +x install-script.sh && ./install-script.sh
```
